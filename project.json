{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/learn-lambda-nestjs/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nrwl/node:webpack",
            "outputs": [
                "{options.outputPath}"
            ],
            "options": {
                "outputPath": "dist/apps/learn-lambda-nestjs",
                "main": "apps/learn-lambda-nestjs/src/main.ts",
                "tsConfig": "apps/learn-lambda-nestjs/tsconfig.app.json",
                "assets": [
                    "apps/learn-lambda-nestjs/src/assets"
                ]
            },
            "configurations": {
                "production": {
                    "optimization": true,
                    "extractLicenses": true,
                    "inspect": false,
                    "fileReplacements": [
                        {
                            "replace": "apps/learn-lambda-nestjs/src/environments/environment.ts",
                            "with": "apps/learn-lambda-nestjs/src/environments/environment.prod.ts"
                        }
                    ]
                }
            }
        },
        "serve": {
            "executor": "@nrwl/node:node",
            "options": {
                "buildTarget": "learn-lambda-nestjs:build"
            },
            "configurations": {
                "production": {
                    "buildTarget": "learn-lambda-nestjs:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": [
                "{options.outputFile}"
            ],
            "options": {
                "lintFilePatterns": [
                    "apps/learn-lambda-nestjs/**/*.ts"
                ]
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": [
                "coverage/apps/learn-lambda-nestjs"
            ],
            "options": {
                "jestConfig": "apps/learn-lambda-nestjs/jest.config.ts",
                "passWithNoTests": true
            }
        }
    },
    "tags": []
}